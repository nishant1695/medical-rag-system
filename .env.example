# Application
APP_NAME=Medical RAG System
APP_VERSION=1.0.0
DEBUG=false

# API
API_HOST=0.0.0.0
API_PORT=8000

# Database
DATABASE_URL=postgresql+asyncpg://medrag:medrag@localhost:5432/medrag

# Redis
REDIS_URL=redis://localhost:6379/0
REDIS_SESSION_TTL=3600

# Vector Store
# Embedded ChromaDB (default, no server needed):
CHROMADB_LOCAL_PATH=./data/chromadb
# HTTP server (only needed if running ChromaDB as a separate service via Docker):
# CHROMADB_LOCAL_PATH=
# CHROMADB_HOST=localhost
# CHROMADB_PORT=8001

# Embeddings
EMBEDDING_MODEL=microsoft/BiomedNLP-PubMedBERT-base-uncased-abstract-fulltext
EMBEDDING_DEVICE=cpu

# LLM Provider
LLM_PROVIDER=anthropic
LLM_MODEL=claude-3-5-sonnet-20241022
LLM_TEMPERATURE=0.1

# API Keys
ANTHROPIC_API_KEY=your-anthropic-api-key
PUBMED_EMAIL=your-email@example.com
PUBMED_API_KEY=your-pubmed-api-key

# Retrieval
NEXUSRAG_VECTOR_PREFETCH=20
NEXUSRAG_RERANKER_TOP_K=8
